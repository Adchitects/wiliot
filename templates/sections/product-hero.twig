{% set subtitle = entry.heroSubtitle ? entry.heroSubtitle : entry.title %}
{% set title = entry.productHeroTitle %}
{% set image = entry.productHeroImage.one() %}
{% if entry is defined %}
	<section class="u-section u-section--product-hero js-bg-shape-hld">
		<div class="u-wrapper--medium-wide u-wrapper">
			<div class="product-hero">
				<div class="bg-shape bg-shape--top-right js-bg-shape" data-version="top-right">
					<img class="s1 js-s1" src="{{ alias('@web/src/img/bg-shape/optimized/top-right-1.png' )}}" alt="Background shape blurry blob">
					<img class="s2 js-s2 js-lazy" data-src="{{ alias('@web/src/img/bg-shape/optimized/top-right-2.png' )}}" alt="Background shape blurry blob">
					<img class="s3 js-s3 js-lazy" data-src="{{ alias('@web/src/img/bg-shape/optimized/top-right-3.png' )}}" alt="Background shape blurry blob">
				</div>
				<div class="product-hero__content">
					{% if subtitle %}
						<span class="product-hero__subtitle u-subtitle u-subtitle--big">{{ subtitle }}</span>
					{% endif %}
					{% if title %}
						<h1 class="product-hero__title u-title u-title--big">{{ title }}</h1>
					{% endif %}
				</div>
				{% if image %}
					<img src="{{ image.getUrl() }}" alt="{{ title }}" class="product-hero__image">
				{% endif %}
			</div>
		</div>
	</section>
{% endif %}
